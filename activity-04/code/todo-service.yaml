# Activity 4 - Step 4.c ("expose service")
# ClusterIP service that fronts the todo container (port 8000) inside the pod.
apiVersion: v1
kind: Service
metadata:
  name: todo
  labels:
    app: todo
spec:
  selector:
    app: todo
  ports:
    - name: http
      port: 8000
      targetPort: 8000
